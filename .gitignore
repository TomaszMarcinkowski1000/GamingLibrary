.claude/settings.local.json

# 10xDevs course materials — kept globally in ~/.claude, never committed here.
# Run `10x get` from C:\Users\lordt\10x-course (skills junctioned to ~/.claude/skills),
# not from this repo, so lesson content never lands here.
.claude/commands/
.claude/skills/
.claude/prompts/
.claude/.10x-cli-manifest.json
skills-lock.json

# from 10x-astro-starter
# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# cloudflare
.dev.vars
.wrangler/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# F-03 spike fixtures: the collector's own shelf photos, their ground-truth labels,
# and generated reports stay local and never enter version control. The format docs
# (README + labels.example.csv) are the only tracked files under this dir.
fixtures/shelf/*
!fixtures/shelf/README.md
!fixtures/shelf/labels.example.csv
